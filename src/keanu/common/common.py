from os import path

import szx81.config as co
from szxxx.common.common import *

UP, DOWN, WAIT, OUTLIERS = (0, 1, 2, -1)

DIR_MAP = {
    UP: 'UP', DOWN: 'DOWN', WAIT: 'WAIT',
    'UP': UP, 'DOWN': DOWN, 'WAIT': WAIT,
    OUTLIERS: 'OUTLIERS'
    }



 